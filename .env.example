# Copier vers .env : cp .env.example .env
# Le .env n'est PAS commite (secrets). Le .env.example l'est (documentation).

POSTGRES_DB=prognize
POSTGRES_USER=prognize
POSTGRES_PASSWORD=change_me_in_local
POSTGRES_PORT=5432
